meta {
  name: Warn
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/collect
  body: json
  auth: inherit
}

body:json {
  {
    "$event": "warn",
    "$params": {
      "message": "Disk space is low"
    },
    "$trace": []
  }
}
